{
  "mcpServers": {
    "onto": {
      "type": "stdio",
      "command": "${CLAUDE_PLUGIN_ROOT}/hooks/onto-serve.sh",
      "args": [],
      "env": {
        "ONTO_PROJECT_DIR": "${CLAUDE_PROJECT_DIR:-.}/.claude/ontology"
      }
    }
  }
}
